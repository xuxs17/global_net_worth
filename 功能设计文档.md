全球收入水平对比计算器 – 功能设计文档
1. 系统概述
本项目为纯前端静态 Web 应用，托管于 GitHub Pages / Netlify，通过预先构建的静态 JSON 文件提供汇率与各国收入基准数据。用户输入月薪及币种后，进行多币种换算、财富等级判定及购买力平价对比，以趣味排行榜和动画角色呈现结果。

2. 技术选型
前端框架：原生 HTML5 + CSS3 + Vanilla JS（可选 Alpine.js 以简化响应式绑定）

数据格式：JSON（rates.json、income_baseline.json）

图表/图片生成：html2canvas（用于分享图）

动画：CSS Animations / Lottie（可选）

数据自动更新：GitHub Actions（定时脚本拉取汇率与基准数据并提交 JSON）

部署：Netlify（自动构建与发布）

3. 模块划分与功能设计
3.1 用户输入模块 (InputModule)
元素	功能	实现要点
金额输入框	仅允许数字及小数点输入，做前端校验（>0）	<input type="number" step="any" min="0.01">
币种下拉	列表包含10个目标币种 + 常见币种（CNY, USD, EUR, GBP, JPY, RUB, INR, VND, IDR, BRL），显示国旗 emoji + 代码	使用国家代码到 emoji 的映射函数生成
计算按钮	触发主计算流程	点击/键盘 Enter 均可触发
示例快捷填入	可预设几个典型月薪值（如 5000 CNY, 3000 USD）作为 Quick Fill 按钮	增加趣味性
3.2 汇率处理模块 (ExchangeModule)
职责：管理内部汇率表，提供基准货币（USD）与各币种的相互转换方法。

数据来源：从 rates.json 加载，格式：

json
{
  "base": "USD",
  "rates": { "CNY": 7.25, "EUR": 0.92, ... }
}
核心函数：

convertToUSD(amount, fromCurrency)：根据 rates[fromCurrency] 计算 USD 值。

convertFromUSD(amountInUSD, toCurrency)：计算目标币种金额。

getAllTargetAmounts(amountInUSD)：返回10个币种的换算结果数组。

3.3 财富等级判定模块 (LevelEngine)
判定依据：各国人均 GNI（存储于 income_baseline.json 的 gni_per_capita 字段）。

年收入计算：annualIncomeUSD = amountInUSD * 12。

倍数计算：ratio = annualIncomeUSD / country.gni_per_capita。

等级映射：

名称（内部）	显示标签（中性网感）	倍数区间
extremely_rich	超高净值人士	> 10
very_rich	相当富裕	(5, 10]
middle	中产水平	(1, 5]
average	普通收入	(0.5, 1]
low	温饱有余	(0.2, 0.5]
very_low	手头有点紧	(0.1, 0.2]
extremely_low	需要精打细算	≤ 0.1
PPP调整判定：pppAdjustedIncome = annualIncomeUSD / country.ppp_conversion_factor，再用同样倍数区间判定 PPP 等级。

3.4 结果排行榜模块 (RankingModule)
对10个目标国家按名义汇率倍数降序排列。

每项数据结构：

javascript
{
  countryCode: "VN",
  currencyCode: "VND",
  flagEmoji: "🇻🇳",
  convertedAmount: 123456789,
  nominalLevel: "extremely_rich",
  pppLevel: "very_rich",
  nominalLabel: "超高净值人士",
  pppLabel: "相当富裕",
  ratio: 15.2
}
渲染为结果卡片列表，每个卡片包含：国旗、币种、金额、两个等级标签、动画角色。

3.5 角色动画模块 (CharacterAnim)
角色资源：7 张 SVG/PNG（或 CSS 绘制的像素角色），按等级 key 映射。

触发时机：结果卡片插入 DOM 后，通过添加 .animate-in 类触发 CSS 动画（如 bounceIn, fadeInUp）。

动画定义示例：

css
.character.extremely_rich { background-image: url('crown-cat.png'); }
.character.very_rich { background-image: url('rich-dog.png'); }
/* ... */
.animate-in { animation: popIn 0.5s ease-out; }
@keyframes popIn {
  0% { transform: scale(0); opacity: 0; }
  80% { transform: scale(1.1); }
  100% { transform: scale(1); opacity: 1; }
}
3.6 分享与状态保持模块 (ShareModule)
URL参数同步：

读取：页面加载时解析 ?amount=5000&from=CNY，自动计算。

写入：输入变更或计算完成后，使用 history.replaceState 更新 URL（debounce 500ms）。

生成分享图：

按钮点击后，用 html2canvas 截取结果区域（.results-container）。

生成 PNG 后提供下载或通过 navigator.share() 分享（移动端）。

在图片底部自动叠加水印“via GlobalSalaryFun”及免责简称。

3.7 免责声明与数据来源模块 (Disclaimer)
固定在页脚，内容包括：

数据仅供娱乐，非财务建议。

汇率与基准数据来源（ECB/Frankfurter, World Bank, IMF）。

最后更新日期（动态读取 rates.json 的 date 字段）。

购买力平价说明。

3.8 广告位模块 (AdModule)
桌面端：侧边栏（结果列表右侧）嵌入 Google AdSense 自适应广告单元，备用 Carbon Ads 脚本。

移动端：结果列表底部插入一个横幅广告。

实现：预留 <div id="ad-container">，根据屏幕宽度动态加载对应广告代码。

4. 数据流设计
text
[页面加载] 
    → 加载 rates.json 和 income_baseline.json 
    → 解析 URL 参数获取 amount, fromCurrency 
    → 若存在参数则自动触发计算
[用户交互]
    → 输入金额/选择币种
    → 点击【计算】或自动触发 (debounce)
    → InputModule 获取 amount, fromCurrency
    → ExchangeModule.convertToUSD(amount, from) → annualIncomeUSD
    → 循环10国：
        - convertFromUSD → 显示金额
        - LevelEngine 判断 nominal / ppp level
    → RankingModule 排序并渲染卡片
    → CharacterAnim 播放入场动画
    → ShareModule 更新 URL
5. 文件结构
text
/
├── index.html          # 唯一页面
├── css/
│   └── style.css       # 样式与动画
├── js/
│   ├── main.js         # 主逻辑、事件绑定
│   ├── exchange.js     # 汇率换算
│   ├── levels.js       # 等级判定
│   ├── render.js       # 榜单渲染
│   ├── share.js        # 分享图与URL
│   └── characters.js   # 角色映射
├── data/
│   ├── rates.json      # 汇率数据（每日Actions更新）
│   └── baseline.json   # 人均GNI/PPP等（季度更新）
└── assets/
    └── characters/     # 等级角色图片/动画资源
6. 非功能性设计
性能：所有数据均内嵌为静态 JSON，首屏加载 < 1.5s，无外部 API 依赖。

可访问性：使用语义化标签，按钮可聚焦，提供适当对比度。

SEO：使用 <title>、<meta description>、Open Graph 标签，利于社交分享时显示预览信息。