全球收入水平对比计算器 – 测试文档
1. 测试策略
单元测试：纯函数逻辑（汇率换算、等级判定）使用 Jest 或简单断言。

集成测试：手动或通过 Cypress 模拟用户操作流程。

兼容性测试：Chrome, Safari, Firefox, Edge 最新版；iOS Safari, Android Chrome。

性能测试：Lighthouse 评分 > 90。

数据更新测试：验证 GitHub Actions 产出 JSON 格式正确且可被前端解析。

2. 测试用例（核心功能）
2.1 输入与计算逻辑
用例ID	场景	输入	预期输出
TC-01	正常计算 CNY	amount=10000, from=CNY	正确换算为 USD 并显示10国金额，年收入=120000CNY → USD约16551（汇率7.25），等级按各国基准判定
TC-02	小数金额	amount=1234.56, from=USD	显示各国金额保留2位小数（日元/越南盾等整数币种可0位）
TC-03	极小值	amount=0.01, from=USD	年收入0.12 → 所有国家均为“需要精打细算”等级
TC-04	极大值	amount=999999999, from=USD	所有国家均为“超高净值人士”，无溢出
TC-05	币种切换	输入1000 CNY，切换为 USD	应重新计算（金额数值不变，币种变化）
TC-06	PPP 判定	某国 PPP 因子 < 1 时	PPP调整后收入大于名义收入，等级可能更高
2.2 排行榜与显示
用例ID	场景	验证点
TC-11	排序正确性	越南、印度等低GNI国家排名靠前，美国、瑞士靠后
TC-12	等级标签显示	使用中性词汇而非“赤贫”等，如“需要精打细算”
TC-13	动画触发	结果卡片出现时，角色有缩放/弹出动画
2.3 分享与URL状态
用例ID	场景	操作	预期
TC-21	URL 参数初始化	访问 ?amount=3000&from=EUR	自动计算并显示结果
TC-22	输入更新 URL	修改金额为 5000	地址栏参数同步更新为 ?amount=5000&from=EUR（无刷新）
TC-23	生成分享图	点击“生成图片”	下载一张包含结果卡片和免责声明的 PNG
TC-24	无效参数忽略	?amount=abc&from=XYZ	显示默认空状态，无报错
2.4 广告与布局
用例ID	场景	验证点
TC-31	桌面端广告位	侧边栏显示广告（可用占位符测试）
TC-32	移动端广告位	结果列表下方出现横幅广告
TC-33	广告不遮挡	所有按钮和金额完全可见可点击
2.5 数据与容错
用例ID	场景	预期
TC-41	rates.json 加载失败	页面显示“汇率数据加载中，请稍后刷新”，不崩溃
TC-42	某国家基准数据缺失	该国家在结果中显示“数据暂缺”或跳过
TC-43	日期显示	页脚显示数据更新日期与 rates.json 中 date 一致
2.6 跨浏览器与响应式
用例ID	场景	验证点
TC-51	移动端布局	卡片宽度自适应，无水平滚动条，按钮不小于44px
TC-52	各主流浏览器	功能正常，动画无闪烁
3. 验收标准
所有上述测试用例通过。

Lighthouse 性能评分 ≥ 90，无障碍评分 ≥ 85。

在 Product Hunt 典型分享场景中，生成的分享图清晰显示核心结果、来源与娱乐声明。

GitHub Actions 每日运行成功，最近一次汇率日期不超过2天。

4. 测试环境
本地：Live Server 打开 index.html

预发布：Netlify Deploy Preview

生产：自定义域名