# 全球收入水平对比计算器 – 产品需求文档 (PRD)

## 1. 项目概述

**产品定位**：一款趣味性多币种收入换算工具，用户输入月薪及币种后，实时显示在10个常见国家/地区的等价金额，并基于当地人均收入/GDP判定其“财富等级”，同时展示购买力平价（PPP）调整后的结果。页面以可爱动画角色配合等级标签，激发用户自发截图分享，实现社交裂变传播。

**核心卖点**：看见你的工资在全球算什么水平（你可以是越南的顶级富豪，却是美国的普通人）。

**变现目标**：初期以网页广告（AdSense + Carbon Ads）作为基础收入，中长期可拓展付费API、企业薪资报告、教学定制等增值服务。

**部署方式**：纯前端静态站点，代码托管在GitHub，通过Netlify部署并绑定自定义域名。

---

## 2. 目标用户

- 对自身收入有好奇心的全球职场人士
- 喜欢在社交媒体分享“身价对比”的年轻人群
- 潜在关注汇率、出国工作、跨境消费的人群
- 经济学、金融学爱好者及教育者

---

## 3. 功能需求

### 3.1 核心计算功能（MVP）

| 功能模块 | 详细描述 |
|--------|--------|
| **用户输入** | 提供数字输入框 + 币种下拉选择器（至少包含10个目标币种：CNY, USD, EUR, GBP, JPY, RUB, INR, VND, IDR, BRL等）。 |
| **汇率换算** | 将用户输入金额及币种先统一折算为内部基准货币（建议USD），再分别换算成10个目标币种，展示对应金额。 |
| **财富等级判定** | 按用户年收入（月薪×12）对标各国人均GNI（或人均GDP），划分为7个等级：极度富有（>10倍）、相当富有（5-10倍）、中产（1-3倍）、普通人（0.5-1倍）、穷人（0.2-0.5倍）、很穷（0.1-0.2倍）、赤贫（<0.1倍）。 |
| **购买力平价对比** | 同时基于世界银行PPP转换因子，显示经购买力调整后的“实际财富等级”，与名义汇率结果并排对比。 |
| **排行榜展示** | 按名义汇率判定的等级从高到低排列10国结果，形成“全球身价排行榜”。 |

### 3.2 交互与趣味性设计

| 功能点 | 详细要求 |
|--------|--------|
| **动画角色映射** | 为7个财富等级分别设计一个可爱的卡通形象（动物/像素人/表情包风格），要求风格统一、无明显文化冒犯。 |
| **角色动画触发** | 当用户点击“计算”按钮后，每个国家的结果卡片中，对应等级的角色以动画形式出现（如弹跳、挥动、摇头等），可使用CSS动画或Lottie实现。 |
| **结果卡片布局** | 每行显示：国旗emoji、币种代码（USD/CNY…）、换算金额、名义汇率等级标签、PPP等级标签、等级动画角色。 |
| **一键分享** | 使用URL参数保存全部输入状态（金额+币种），用户复制当前URL发给他人即可看到完全相同的计算结果。另提供“生成分享图片”按钮，将当前结果卡片区域用Canvas渲染成图片保存。 |
| **实时收入计时器（可选V2）** | 输入时薪后，点击“开始计时”，页面以秒为单位显示累计收入数字，增强停留时长。 |

### 3.3 非功能性要求

| 类型 | 说明 |
|------|------|
| **免责声明** | 页面底部固定展示免责声明，明确注明数据来源、仅为娱乐参考、不构成财务建议，并附上最后更新日期。 |
| **数据来源标注** | 在结果页或设置中注明汇率基准（欧洲央行 / Frankfurter API）、人均数据来源（世界银行、IMF、CEOWORLD）。 |
| **响应式设计** | 需兼容手机、平板、桌面，移动端优先。 |
| **性能** | 所有汇率和数据从仓库内静态JSON文件读取，无第三方API实时请求，页面加载即用。 |

---

## 4. 数据方案

### 4.1 汇率数据

- **来源**：Frankfurter API（免费、无Key、以ECB汇率为准）
- **更新方式**：通过GitHub Actions每天UTC 0点运行脚本，调用API获取以USD为基准的全币种汇率（若Frankfurter以EUR为基准，脚本应转换），产出`rates.json`提交至仓库。
- **备用方案**：若Frankfurter不支持VND、IDR等小币种，补充使用ExchangeRate-API（免费额度1500次/月，可在Actions中定时调用）。

### 4.2 收入基准数据

- **人均GNI**：世界银行API（免费、无需认证），每年更新一次，可在Actions中设定每季度拉取并缓存。
- **人均GDP**：IMF WEO 2025年4月版，手动下载CSV存放于仓库`data/imf_gdp_2025.csv`或通过简易脚本转换。
- **PPP转换因子**：世界银行ICP数据（LCU per international dollar），同样每年更新，存储为`ppp.json`。
- **月度平均薪资（趣味维度）**：CEOWORLD 2025排行榜数据，手动整理为`average_salary.json`备用（用于在结果中展示“该国平均月薪约XXXX”增加话题性）。

### 4.3 数据接口设计（前端读取）

所有数据在构建时以静态JSON文件存在仓库，前端按下列格式请求：

```json
// rates.json
{
  "base": "USD",
  "date": "2026-05-07",
  "rates": {
    "CNY": 7.25,
    "VND": 25400,
    ...
  }
}

// income_baseline.json
{
  "US": {
    "gni_per_capita": 76000,
    "gdp_per_capita": 89000,
    "ppp_conversion_factor": 1.0,
    "average_monthly_salary": 6562
  },
  ...
}

## 5. 技术架构与实现细节

### 5.1 架构总览

- **前端**：纯静态 HTML5 + CSS3 + Vanilla JS（或轻量框架如 Alpine.js / Petite-Vue），无后端。
- **数据更新**：GitHub Actions 定时任务（cron） → 生成/更新静态 JSON → 提交到仓库。
- **部署**：GitHub 仓库关联 Netlify，分支推送自动部署。
- **广告接入**：在结果页尾部和侧边栏预留广告位，初期通过 Google AdSense 和 Carbon Ads 填充。

### 5.2 主要算法逻辑

1. 获取用户输入金额 `amountInput` 和币种 `fromCurrency`。
2. 从 `rates.json` 查 `fromCurrency` 对 USD 的汇率，计算 `amountInUSD = amountInput / rateFrom`。
3. 遍历 10 个目标币种，计算 `convertedAmount = amountInUSD * rateTo`。
4. 年收入 `annualIncomeUSD = amountInUSD * 12`。
5. 读取该国人均 GNI 值 `gni`，计算倍数 `ratio = annualIncomeUSD / gni`，按区间映射等级。
6. 读取该国 PPP 因子 `pppFactor`，计算 `pppAdjustedIncome = annualIncomeUSD / pppFactor`，再与人均 GNI 比较得出 PPP 等级。
7. 按比率降序排列输出卡片。

### 5.3 前端实现要点

- **币种下拉**：使用 `<select>` 配合国旗 emoji（可通过区域码生成，如 🇺🇸 USD）。
- **URL 状态同步**：用 `URLSearchParams` 读写 `amount` 和 `from` 参数，监听输入变化实时更新地址栏（debounce 500ms），便于分享。
- **动画**：为每个等级预定义 CSS 类（如 `.level-extremely-rich`），通过 `classList` 切换触发动画（transition/animation）。
- **生成分享图**：使用 `html2canvas` 库将结果卡片区域转为 PNG，通过 `navigator.share()` 或直接下载实现。

---

## 6. UI/UX 设计指南

### 6.1 页面结构

```text
+-----------------------------+
| Logo / 标题                  |
+-----------------------------+
| [数字输入框] [币种选择▼]    |
| [计算按钮]                   |
+-----------------------------+
| “你的全球身价排行榜”         |
+-----------------------------+
| 🇺🇸 USD  0.00  [普通人] 🐱  |
| 🇬🇧 GBP  0.00  [中产]  🐶   |
| 🇯🇵 JPY  0.00  [相当富有] 🦊 |
| ...                         |
+-----------------------------+
| 免责声明                     |
| Powered by xxx  ©2026       |
+-----------------------------+
```

### 6.2 风格与色调

- **总体风格**：明亮、活泼、轻拟物感或扁平化，背景可使用柔和渐变（如浅蓝到浅紫）。
- **角色设计**：建议采用表情包或扁平可爱动物形象，避免种族/地域特征。可用免费图标库如 OpenMoji 经二次上色处理。
- **移动端适配**：卡片堆叠，横向滚动结果条，字体适中，按钮大于 44×44pt。

---

## 7. 广告与变现策略

### 7.1 初期广告接入

- **Google AdSense**：在结果页底部、侧边栏（桌面端）嵌入自适应广告单元。
- **Carbon Ads**：专门面向开发者/设计人群，可将一个广告位设置为”仅显示 Carbon Ads”，若未填充则回退到 AdSense。
- **注意**：确保广告不遮挡核心交互按钮，不自动播放音频视频。符合 Netlify 免费计划的合理货币化政策。

### 7.2 长期变现拓展（不在 MVP 范围）

- **付费 API**：提供”多币种购买力对比”JSON API，按调用量收费。
- **嵌入式组件**：提供 iframe 或 Web Component，供合作网站嵌入，附带广告或 brand logo。
- **企业薪资报告**：生成定制化 PDF 报告，按次或按年付费，面向出海企业 HR。
- **捐赠/打赏**：页脚放置 Buy Me a Coffee 链接。

---

## 8. 风险控制与合规

| 风险项 | 应对措施 |
|--------|----------|
| 财富等级用词可能冒犯 | 使用中性网感词汇：”你在这里属于超高净值人群””按当地标准属于小康水平”，避免”穷””赤贫”等直接标签。 |
| 数据被当作真实收入指导 | 页面及分享图显著位置标注”娱乐参考，非专业建议”。 |
| 广告拦截器影响收入 | 初始设置为非侵入式广告，并接受较低广告填充率，将主要精力放在社交裂变带来流量上。 |
| API 停止维护或数据更新延迟 | 关键数据缓存至仓库，即使远程 API 失效，仍可使用最近一次更新数据，并显示更新日期。 |

---

## 9. 开发与上线路线图（Vibe Coding 分步实现）

### 第一阶段：基础计算器

- 搭建静态页面框架（HTML + CSS + JS）
- 实现输入、下拉选择、汇率换算（使用本地静态 JSON 模拟）
- 展示 10 国换算结果
- 部署到 Netlify 测试

### 第二阶段：等级判定与趣味性

- 加入人均收入/PPP 数据和判定逻辑
- 设计 7 个等级动画角色形象（可请设计师或使用 AI 生成素材）
- 为每个结果卡片添加角色动画
- 实现 URL 状态共享

### 第三阶段：数据自动化与广告

- 创建 GitHub Actions 定时更新汇率脚本
- 集成世界银行/IMF 数据静态化
- 接入 Google AdSense 和 Carbon Ads
- 生成分享图功能

### 第四阶段：优化与推广

- 移动端体验优化，性能检查
- 在 Product Hunt、X（Twitter）、Reddit 等平台发布
- 跟踪用户行为（简单计数器或 Plausible Analytics）并根据反馈迭代
- 探索付费 API 或企业版本

---

**文档版本**：v1.0
**最后更新**：2026 年 5 月 7 日